// import { useQuery } from '@tanstack/react-query';
// import { getMenuCategories } from 'api/store';

const useMenuCategories = () => {
  // const { data } = useQuery(['menuCategory', id], () => {
  // // getMenuCategories(id)
  //   if (id) { // id가 존재하는 경우에만 getMenuCategories 함수 호출
  //     return getMenuCategories(id);
  //   }
  //   return null; // id가 null인 경우에는 null을 반환하여 data가 null이 되도록 함
  // });
  // return {
  //   data,
  // };
};

export default useMenuCategories;
